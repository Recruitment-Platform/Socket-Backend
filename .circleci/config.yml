version: 2.1

orbs:
  gradle: circleci/gradle@3.0.0
  tcc: atomicjar/testcontainers-cloud-orb@0.1.0

executors:
  jdk17:
    docker:
      - image: cimg/openjdk:17.0

workflows:
  build-and-deploy:
    jobs:
      - gradle/test:
          executor: jdk17
          pre-steps:
            - tcc/setup

